Write the middleware which can change the authentication machanism based on the tenant congig.

Write the cron to notify the stock availability
Red alert if they are below threshold.

Reports for in and out.
